# Production Environment Variables
# IMPORTANT: This file should NEVER be committed to version control
# Copy this to .env and update with your actual production secrets

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
DB_NAME=absensi_wfh_dexa

# JWT Configuration
# CRITICAL: Generate a strong random secret with: openssl rand -base64 32
# Never use the same secret across different environments!
JWT_SECRET=CHANGE_THIS_IMMEDIATELY_USE_OPENSSL_RAND_BASE64_32
JWT_EXPIRES_IN=24h

# Service Ports
AUTH_SERVICE_PORT=3001
PROFILE_CHANGE_LOG_SERVICE_PORT=3002
ATTENDANCE_SERVICE_PORT=3003
NOTIFICATION_SERVICE_PORT=3004

# Environment
NODE_ENV=production
